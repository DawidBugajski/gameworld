import React from 'react';

const FullscreenGallery = ({ screenshots }) => {
  console.log(screenshots);
  return (
    <div className='fixed inset-0 flex items-center justify-center text-center text-white bg-gray-800 text-9xl z-[100]'>
      FULLSCREEN GALLERY COMPONENT
    </div>
  );
};

export default FullscreenGallery;
